# Local edge-function secrets. Copy to supabase/functions/.env and fill in.
# This file is .gitignored — never commit the real key.
#
# Production secrets are managed in the Supabase dashboard:
#   Project → Edge Functions → Secrets

# Used by moderate-comment for AI moderation (claude-haiku-4-5-20251001).
# Get a key at https://console.anthropic.com/.
ANTHROPIC_API_KEY=
