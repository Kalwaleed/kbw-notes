// The public reader feed now lives at /kbw-notes/home, with / redirecting
// there. Route-level coverage lives in protected-routes.spec.ts until this
// file gets seeded-data assertions for feed cards and post navigation.
//
// The replacement coverage now lives in:
//   - protected-routes.spec.ts  public reader routes + hidden auth/admin routes
//   - security-rls.spec.ts      RLS boundary (anon key)

export {}
